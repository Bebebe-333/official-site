# 再構築計画：「用紙ファースト」のプロフェッショナルUI

この計画の目標は、「デジタルペーパー（電子用紙）」のコンセプトでアプリケーションを再構築することです。画面上のUIをA4用紙のレイアウトに厳密に一致させることで、完全なWYSIWYG（見たままが得られる）体験を実現し、印刷時のレイアウト崩れを排除します。

## コアコンセプト
-   **WYSIWYG**: メイン画面には中央に配置されたA4用紙そのものを表示します。
-   **厳格なグリッド**: レイアウトが崩れないよう固定された「10行 × 4列」のグリッドを採用します。
-   **プロフェッショナルな美学**: 黒の実線ボーダー、清潔感のある明朝/ゴシックフォント、高密度の情報配置。

## 変更内容案

### [HTML] index.html
-   **ボディ構造の置換**:
    -   メインラッパーとして `<div class="a4-sheet">` （固定寸法）を使用。
    -   内部構造:
        -   **ヘッダー**: タイトル、日付、文書情報。
        -   **製品情報グリッド**: 隙間なく高密度に配置。
        -   **工程一覧テーブル**: コアとなる10×4グリッド。
        -   **フッター**: 最小限のコピーライト/ページ番号。
    -   「操作バー」（保存/印刷ボタン）はA4用紙の外側にフローティング配置。

### [CSS] style.css
-   **リセット**: スタイルを完全にリセット。
-   **ページ寸法**: `.a4-sheet { width: 210mm; height: 297mm; }`
-   **画面表示**: 背景グレー、中央に白い用紙＋ドロップシャドウ。
-   **印刷表示**: 背景、シャドウ、用紙外の余白を削除。
-   **グリッドシステム**: CSS Gridによる厳密な整列。
-   **タイポグラフィ**: 伝統的なビジネスフォント（Helvetica, Arial, Noto Sans JPなど）。

### [JS] script.js
-   **状態管理**: 常に正確に10個のアイテムを持つ配列を維持。
-   **描画ループ**: データが空でも、常に10行分の入力欄を描画。
-   **永続化**: 厳格な構造をそのまま保存/読み込み。

## 検証計画
1.  **画面表示**: まるで紙の書類のように見えるか確認。
2.  **印刷**: `Ctrl + P` の出力結果が画面と *完全に* 一致するか確認。
3.  **データ**: 保存/読み込みを行っても10行の構造が維持されるか確認。
